# LaunchDock 项目协作说明

## 默认语言

本项目的所有需求文档、说明文件、注释、界面文案、错误提示、配置说明和后续新增文件，默认使用中文。

只有在以下情况可以使用英文：

- 第三方库、API、命令、文件名或协议本身使用英文。
- 代码标识符需要遵循常见英文命名习惯。
- 文件名、目录名需要面向开源协作、跨系统兼容和工具识别时，应使用简洁、合适的英文命名。
- 用户明确要求输出英文。

## 文件命名

后续新增文件和目录时，文件名默认使用简洁、合适的英文名称，例如 `development-log.md`、`project.json`、`launchdock.json`。

文件内容仍按“默认语言”要求使用中文。已有中文文件名可以暂时保留，除非用户明确要求重命名。

## 项目定位

LaunchDock 是一个本地轻量级“启动坞”。它用于管理学习、工作或个人项目中的常用网页链接，并支持一键打开某个项目所需的全部默认链接。

用户每天上线学习或工作时，经常需要重复打开视频课程、Notion 笔记、官方文档、代码仓库、练习平台等页面。LaunchDock 的目标是把这些重复动作整理成可复用的项目模块，让用户选择一个项目后即可快速进入对应工作状态。

## 核心用户目标

- 用户可以自行创建多个项目模块。
- 每个项目模块可以保存多个需要打开的网页链接。
- 创建项目时可以按需添加该项目的启动链接，也可以先创建空项目，后续再补充链接内容。
- 用户可以后续继续添加、编辑、删除、排序链接。
- 用户可以一键启动某个项目，自动打开该项目中标记为默认启动的链接。
- 所有项目数据保存在本地，关闭程序后不丢失。
- 数据结构应清晰，方便用户后续手动备份、迁移和理解。

## 项目专属 Skill

本项目包含一个只服务于 LaunchDock 的项目专属 Skill：

`.agents/skills/launchdock-project/SKILL.md`

该路径必须保持为仓库相对路径，不能写成本机绝对路径。项目未来会上传到 GitHub 开源，其他用户下载仓库后应能在自己的系统中通过相对路径找到该 Skill。

使用场景：

- 更新 LaunchDock 的需求、产品设计或开发约定。
- 设计项目模块、链接管理、启动坞、本地存储结构。
- 实现或调整创建项目、删除项目、编辑链接、一键启动等重复性工作。
- 维护本项目的中文文档和中文界面文案。

这个 Skill 不应作为全局通用技能使用，也不应服务其他项目。

后续如果新增、修改、删除或移动任何 Skill，必须同步更新本文件中的项目专属 Skill 说明，确保 `AGENTS.md` 始终反映当前仓库实际可用的 Skill 状态。

## 功能需求

### 项目管理

- 支持创建项目。
- 支持在创建项目时直接添加该项目需要启动的网页链接。
- 支持只填写项目名称并创建空项目，允许用户之后再补充链接和说明。
- 支持查看所有项目。
- 支持重命名项目。
- 重命名项目时，应同步重命名该项目在启动坞 `projects/` 下的本地文件夹，并更新 `project.json` 里的 `folder_name`；如目标文件夹名已存在，应自动生成不冲突的文件夹名，不能覆盖其他项目。
- 支持删除项目。
- 支持启动项目管理模式：用户可以勾选多个启动项目，并批量删除。
- 启动项目管理模式中应提供“全选”“取消全选”“反选”能力，方便批量选择启动项目。
- 删除项目前应有明确确认，避免误删用户数据。

### 链接管理

每个项目可以包含多个链接。每个链接至少包含：

- 链接名称，例如“视频课程”“Notion 笔记”“官方文档”“GitHub 仓库”。
- URL 地址或本地文件路径。
- 是否默认启动。
- 排序位置。

应支持：

- 添加链接。
- 编辑链接名称。
- 编辑链接 URL。
- 设置是否默认启动。
- 删除链接。
- 调整链接顺序。
- 支持单个项目内的启动项管理模式：用户可以勾选该项目内多个启动项，并批量删除。
- 启动项管理模式中应提供“全选启动项”“取消全选”“反选”能力，方便批量选择该项目内的启动项。

### 一键启动

- 用户选择某个项目后，可以一键打开该项目的默认启动链接。
- 网页链接默认在浏览器新标签页中打开。
- 本地文件或文件夹路径应交给系统默认程序打开。
- 如果项目中没有默认启动链接，应给出清晰提示。
- 如果目标为空、URL 格式明显异常或本地路径不存在，应避免静默失败，并提示用户检查。

## 本地启动坞设计

LaunchDock 的用户数据应保存在一个“启动坞”大文件夹中。

### 启动坞路径

- 启动坞路径应支持用户自定义。
- 用户可以选择一个本地文件夹作为启动坞根目录。
- 用户首次下载并启动软件时，不应自动创建默认启动坞路径；初始状态应为空。
- 没有创建或选择启动坞前，用户不能创建启动项目。
- 没有启动坞时，主界面应提示用户先创建启动坞，并说明启动坞用于保存启动项目的存储位置。
- 后续应支持下载、导出或迁移这个启动坞文件夹，方便换电脑或备份。
- 程序需要记住用户选择的启动坞路径。

### 启动坞结构

所有小项目都保存在启动坞大文件夹下，每个小项目单独占用一个子文件夹，方便管理。

建议结构：

```text
用户选择的启动坞文件夹/
  launchdock.json
  projects/
    pytorch/
      project.json
    linear-algebra/
      project.json
    english-reading/
      project.json
```

说明：

- `launchdock.json` 保存全局配置，例如数据版本、项目排序、最近打开项目、窗口设置等。
- `projects/` 保存所有小项目。
- 每个项目一个独立文件夹。
- 每个项目文件夹内保存该项目自己的 `project.json`。
- 项目文件夹名称可以基于项目名生成，但需要处理重名、空格、特殊字符和中文名称。

### 项目数据示例

```json
{
  "id": "uuid",
  "name": "PyTorch 学习",
  "created_at": "2026-06-14T09:00:00+08:00",
  "updated_at": "2026-06-14T09:30:00+08:00",
  "links": [
    {
      "id": "uuid",
      "name": "官方文档",
      "url": "https://pytorch.org/docs/stable/",
      "default_open": true,
      "order": 1
    }
  ]
}
```

## 界面风格方向

启动坞窗口使用 PySide6 + QFluentWidgets 实现，风格参考 `ok-oldking/ok-wuthering-waves` 和 `ok-oldking/ok-script` 的 Qt/Fluent 桌面应用架构：

参考链接：[ok-oldking/ok-wuthering-waves](https://github.com/ok-oldking/ok-wuthering-waves)

架构参考：[ok-oldking/ok-script](https://github.com/ok-oldking/ok-script)

可借鉴方向：

- 工具型桌面应用，而不是营销页或重装饰页面。
- 强调“一键启动 / 一键日常”的主操作。
- 整体为深色主题，类似 Windows 11 Settings / WinUI 3 风格。
- 使用接近 Segoe UI 的字体。
- 左侧是垂直导航栏，包含图标和文字。
- 左侧导航栏展开宽度应根据当前语言下导航名称长度自适应，并设置合理上下限；侧边栏可使用比页面标题更短的导航专用名称，避免中文界面过宽，也避免英文、西班牙语等较长文本溢出。
- 右侧主区域使用设置页面布局。
- 每一项设置或启动项使用横向圆角卡片。
- 卡片左侧包含图标、标题和说明文字。
- 卡片右侧包含开关按钮、编辑图标、下拉箭头或主要操作按钮。
- 背景颜色低对比度，整体不要太花。
- 使用青色作为强调色，例如开关开启状态、选中状态和主要按钮。
- 视觉效果要像 Windows 桌面软件，而不是网页后台管理系统。
- 主界面右上角有“新建”按钮，用于创建启动项目。
- 每一个启动项目应以卡片形式显示，并在主界面整齐排列。
- 每一个项目卡片右上角有编辑图标，点击可编辑项目并显示该项目需要启动的链接内容。
- 项目卡片右上角不应提供与卡片底部“添加启动项”重复的添加按钮。
- 项目卡片应提供折叠按钮，点击后展示全部启动项，再次点击后收起启动项。
- 每一条启动项右侧有开关按钮，用于控制是否参与启动。
- 每一条启动项右侧也应有编辑图标，用于编辑该链接或文件目标的名称与地址。
- 启动项列表中默认只显示启动项名称，不直接展示完整 URL 或本地路径，避免长地址撑开卡片并影响“启动”按钮可见性；完整地址在编辑对话框中查看和修改。
- 启动项不显示上移、下移箭头；如果后续需要排序，应设计清晰可用的排序交互后再加入。
- 每一个启动项目卡片正下方有启动按钮，点击后打开该启动项目内所有启用的链接、地址或文件。
- 顶部提供“管理项目”功能，进入后可勾选启动项目，并支持全选、取消全选、反选和批量删除。
- 每个项目卡片提供“管理启动项”功能，进入后可勾选该项目内启动项，并支持全选、取消全选、反选和批量删除。
- 项目卡片区域背景应保持透明或与主区域一致，不要出现突兀的大块黑色矩形背景。
- 多语言界面中，语言下拉列表里的各语言名称应使用对应语言自身的写法，例如 `English`、`日本語`、`한국어`、`Español`；只有“使用系统设置”这一项需要跟随当前界面语言变化。
- 操作按钮应直观，主按钮突出。
- 整体观感可以偏现代、紧凑、稳定，避免花哨动画和过度装饰。
- 软件启动后默认显示“关于”页面，而不是启动项目页面。
- “关于”页面保留页面标题，并使用横向信息卡片展示软件图标、名称、当前版本号、检查新版本、GitHub 和分享下载链接按钮。
- 更新检查通过 GitHub Releases 获取最新发布版本；如果存在新版本，应显示当前版本到最新版本的版本跨度，并展示 GitHub Release 正文作为新版本改动内容。
- 软件启动后应延迟自动检查更新；只有发现新版本时才弹窗提醒用户是否打开下载页面，用户可在本次选择不更新。
- 手动点击“检查新版本”时，应在“关于”页面显示检查状态、无 Release、已是最新版本、检查失败或新版本内容。

不得照搬参考项目的业务内容、图标、品牌素材或游戏相关元素。只参考其桌面工具应用的组织方式和简洁操作感。

## 技术偏好

- 首选 Python。
- UI 层使用 PySide6 + QFluentWidgets。
- 不再使用 Tkinter 实现主界面。
- UI 层集中在 `launchdock/app.py`，业务模型和存储逻辑分别保留在 `launchdock/models.py` 和 `launchdock/storage.py`。
- 修改 UI 时优先保持 `models.py` 和 `storage.py` 不变，除非确实需要新增业务能力。
- 打开网页链接优先使用 Python 标准库 `webbrowser`。
- JSON 读写必须使用结构化 API。
- 不使用临时字符串拼接来伪造 JSON。
- 代码结构要适合初学者阅读和修改。
- 注释保持中文，且只解释不明显的逻辑。
- 文档中的仓库内路径应使用相对路径，不要写死任何开发者本机路径。
- 用户数据路径必须由用户选择或由程序根据系统环境生成，不能写死为某台电脑上的绝对路径。

## 应用图标

- 软件图标源文件为 `assets/icon.png`。
- 应用运行时应使用该文件作为窗口图标。
- 图标源推荐使用带透明通道的 PNG；如果源图缺少透明通道，运行时可以基于角落背景色临时剔除纯色背景，但不应覆盖或修改原始图标源文件。
- 应用窗口标题栏显示 `LaunchDock`，不追加中文后缀。
- 后续打包为桌面软件时，也应使用该文件作为软件图标源。
- 如果打包工具要求 `.ico` 等格式，应从 `assets/icon.png` 派生生成，不要替换原始图标源。

## 质量要求

- 用户数据优先，不能随意覆盖或删除。
- 删除项目、删除链接、迁移启动坞等操作应有保护机制。
- 对缺失、损坏或版本不匹配的数据文件，要给出可理解的中文提示。
- 后续如涉及数据结构迁移，应先备份原数据。
- 文件路径处理要兼容 Windows。
- 用户自定义启动坞路径可能包含中文、空格或特殊字符，代码实现时必须考虑。
- 开源仓库中的说明、配置和示例不能依赖维护者本机路径。

## 开发记录

后续每次生成或修改代码时，都需要同步更新 `development-log.md`。

记录内容应包括：

- 修改日期和具体时间。
- 修改范围。
- 新增、修改或删除了哪些文件。
- 具体做了什么。
- 是否运行了验证或测试。
- 如果没有运行验证或测试，需要说明原因。

版本号（例如 `0.2.0`）仅在正式完成一轮开发、重新打包或上线更新时才记录到开发日志。日常开发和测试过程中的开发日志条目只需要日期和时间，不使用版本号。

如果某次更新需要升级项目版本号，应在开发日志标题中标注版本号，格式如 `## Version 0.2.0 - 2026-06-20 10:00:00 +08:00`，并同步更新 `launchdock/__init__.py` 中的 `__version__`。日常开发和测试中的修改不改变 `__version__`。

这个文件用于帮助用户回顾项目演进，也帮助 Codex 等代码助手理解此前做过的工作。

## 发布流程

- GitHub 仓库地址为 `https://github.com/Dylanliiiii/LaunchDock`。
- 正式发布前，先按常见语义化版本规则确认版本号：主版本号用于大改或不兼容变更，次版本号用于新功能，修订号用于 bug 修复。例如当前版本为 `1.0.0` 时，大改发布为 `2.0.0`，新功能发布为 `1.1.0`，bug 修复发布为 `1.0.1`。
- 正式发布时，需要同步更新 `launchdock/__init__.py` 中的 `__version__`。
- 正式发布时，需要在 `development-log.md` 顶部新增 `## Version x.x.x - YYYY-MM-DD HH:mm:ss +08:00` 记录。
- Release 正文应从上一次带 `Version` 的发布记录之后，到本次发布记录之间提炼重点，不应过长；小型格式、文案和布局调整用概括性描述即可。
- Windows 打包使用 `scripts/build-windows.ps1`，该脚本从 `assets/icon.png` 临时派生打包所需图标，不覆盖原始图标源文件。
- Windows 安装包使用 `scripts/build-installer.ps1` 和 Inno Setup 6 生成，安装包文件名格式为 `LaunchDock-v版本号-windows-setup.exe`。
- 打包产物位于 `dist/`，不得将用户本机启动坞数据或本地配置文件包含到 Release 包中。

## 未来可选功能

- 导入 / 导出整个启动坞。
- 每个项目记录学习时长。
- 为项目设置每日提醒。
- 为链接添加标签或分组。
- 支持搜索项目和链接。
- 支持最近启动项目。
- 支持项目启动前预览本次将打开哪些链接。

## 原始需求文档

原始需求来自 `学习项目启动器-需求文档.md`，其核心内容已经合并到本文件。

用户后续可以删除原始需求文档，保留本 `AGENTS.md` 作为主要协作说明。

